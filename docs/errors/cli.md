---
title: その他 CLI のエラー
---

# その他 CLI のエラー

AWS CLI / kubectl / docker / mkdocs / gh / openssl など、雑多なエラーを貼り付ける場所です。

```bash
# 例: kubectl の権限エラー
Error from server (Forbidden): pods is forbidden: User "..." cannot list resource "pods" in API group "" in the namespace "default"
```
